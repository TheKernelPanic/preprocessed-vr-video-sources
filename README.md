# Task for preprocessed vr videos from any sources

## Run docker

```bash
docker run -dit -v "%cd%:/usr/share/workdir" --workdir "/usr/share/workdir" --name "preprocessed-vr-videos" ubuntu:20.04 
```